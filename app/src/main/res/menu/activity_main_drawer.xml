<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:showIn="navigation_view">

    <group android:checkableBehavior="single">
        <item
            android:id="@+id/nav_home"
            android:icon="@drawable/ic_home" <!-- Necesitarás ic_home -->
            android:title="Resumen" />
        <item
            android:id="@+id/nav_stats"
            android:icon="@drawable/ic_stats" <!-- Necesitarás ic_stats -->
            android:title="Estadísticas" />
        <item
            android:id="@+id/nav_history"
            android:icon="@drawable/ic_history" <!-- Necesitarás ic_history -->
            android:title="Historial" />
    </group>

    <item android:title="Otros">
        <menu>
            <item
                android:id="@+id/nav_settings"
                android:icon="@drawable/ic_settings" <!-- Necesitarás ic_settings -->
                android:title="Configuración" />
        </menu>
    </item>
</menu>
